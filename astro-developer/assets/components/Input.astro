---
const { label = "Label", name = "field", placeholder = "Enter value", class: className = "", type = "text" } = Astro.props;
---
<label class="block text-sm font-medium text-gray-700">
  {label}
  <input
    class={`mt-1 w-full rounded-md border border-gray-300 px-3 py-2 text-sm shadow-sm focus:border-black focus:outline-none focus:ring-1 focus:ring-black ${className}`.trim()}
    type={type}
    name={name}
    placeholder={placeholder}
  />
</label>